source 'https://rubygems.org'
# Declaramos aqui mesmo a gem rspec no bundler do nosso arquivo
# gem install bundler
# Com o bundler já instalado o rspec já vem automaticamente com o comando bundle install

gem 'rspec'